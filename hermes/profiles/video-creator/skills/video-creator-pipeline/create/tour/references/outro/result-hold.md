# Result Hold

Role: make completion unambiguous. Default ON, not mandatory. Appearance: final
UI stays visible with one concise completion line. Inputs: approved result and
completion copy. Motion: last action settles, completion arrives, then a short
readable hold; no black fade or idle wobble. QA: actual result remains visible
through the final frame, copy fits, hold is long enough for the chosen language.

# Outline

For authored v2: crisp presentation contours, restrained solid fills and clear
focus outlines. Color and radius follow the approved local direction, not
mandatory white outlines. Faithful product internals retain their own styling.

Legacy v1 rendering: white device outline, square label
plate, accent target border, no backdrop blur, 90% black scrim over the
static cover-fit backdrop. The accent is never the label-text foreground.
Without a backdrop, the background stays undimmed with luminance-selected ink.
Check source UI readability separately from authored-label contrast.

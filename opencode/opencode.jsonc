{
  "$schema": "https://opencode.ai/config.json",
  "plugin": [
    // claude-auth: OpenCode runs on the SUB Claude account via the Keychain entry
    // "Claude Code-credentials-<suffix>" (~/.claude-sub, selected in
    // ~/.local/share/opencode/claude-account-source.txt). The DEFAULT entry
    // ("Claude Code-credentials") belongs to Hermes — see hermes/AGENTS.md.
    // Pinned (not @latest): OpenCode's packages cache never re-resolves @latest,
    // which left us on 1.5.4 for months and caused daily auth expiry. 2.1.x adds
    // proactive token refresh (#238), recovery after external credential
    // rotation (#252), and CLAUDE_CONFIG_DIR support (#239). Bump manually.
    // Verified against 2.1.6 dist: claude-account-source.txt still selects the
    // active account at init, and the CLI-fallback refresh auto-resolves the
    // suffix hash to ~/.claude-sub (no CLAUDE_CONFIG_DIR needed).
    // CAVEAT (borrowing): if the SUB account cannot be refreshed, the last
    // resort still BORROWS another account's credentials — 2026-08-06..08 that
    // silently ran a long-lived `opencode serve` on the Hermes account for two
    // days. 2.1.6 makes the borrow RECOVERABLE (borrowedCredentialAccounts:
    // borrowed tokens are never used as a refresh source, never written back to
    // the borrower's store, and are released once the real account recovers) but
    // does NOT prevent it. Detection: CLAUDE_AUTH_DEBUG is exported globally in
    // zsh/env.zsh — grep "refresh_fallback_account" in
    // ~/.local/share/opencode/claude-auth-*.log, then re-auth via `claude-sub`
    // promptly. Ground truth is auth.json's token vs the Keychain entry, not
    // this file.
    // CAVEAT (stray Keychain entries): refreshViaCli() exports
    // CLAUDE_CONFIG_DIR=<configDir> and runs `claude -p .`, so Claude Code mints
    // a NEW Keychain item named Claude Code-credentials-<sha256(configDir)[:8]>.
    // That is how "Claude Code-credentials-bbc17b0c" (= ~/.claude = Hermes,
    // duplicate of the unsuffixed entry) appeared and turned into a wrong-account
    // borrow target; deleted 2026-08-08. `opencode auth login` lists one option
    // per Keychain item, so a third option reappearing means a stray was minted
    // again — check `security dump-keychain | grep "Claude Code-credentials"`.
    // LOCALLY PATCHED (2026-08-11): 2.1.6's direct OAuth refresh POSTs to
    // claude.ai/v1/oauth/token with no User-Agent, which Anthropic 429s
    // (rate_limit_error, no Retry-After) persistently — 157/157 refresh
    // attempts failed in claude-auth-*.log, forcing manual `claude-sub /login`
    // every ~8h token lifetime (upstream issue #270). Patched cached dist
    // (~/.cache/opencode/packages/opencode-claude-auth@2.1.6/.../dist/credentials.js):
    // OAUTH_TOKEN_URL → https://platform.claude.com/v1/oauth/token (current
    // Claude Code endpoint; claude.ai is stale) + User-Agent
    // "claude-cli/2.1.220 (external, cli)". Re-apply after any version bump,
    // and keep the UA version roughly in sync with `claude --version`.
    // Verify: refresh_success in claude-auth-*.log at the next refresh window.
    // NOTE (2026-09-02): upstream 2.2.0 STILL points OAUTH_TOKEN_URL at the stale
    // claude.ai host with no User-Agent, so bumping does not retire this patch.
    // CLIENT VERSION (2026-09-02): the plugin also spoofs the Claude Code client
    // version on normal API calls (user-agent + cc_version in the billing system
    // header), hardcoded as config.ccVersion in dist/model-config.js — 2.1.6 says
    // 2.1.217. Anthropic gates newer models on a minimum client version, so that
    // stale value hard-fails them ("Claude Code 2.1.217 does not support this
    // model; version 2.1.251 or newer is required"). NOT patched here: env.zsh
    // exports ANTHROPIC_CLI_VERSION, the plugin's own override, derived from the
    // real `claude` install so it tracks updates and survives cache re-resolves.
    // (2.2.0 would raise ccVersion to 2.1.257, but the override makes that moot.)
    "opencode-claude-auth@2.1.6",
    // quota: pinned — its cached dist/lib/anthropic.js is locally PATCHED to read
    // the sub-account Keychain entry above (upstream hardcodes the default entry
    // and would display Hermes' usage). Re-apply the patch after a version bump.
    "@slkiser/opencode-quota@3.11.1"
  ],
  "instructions": [
    "instructions/*.txt",
    "instructions/*.md",
    // Private-overlay instructions (silently empty on machines without ~/.config/private).
    "../private/opencode/instructions/*.md"
  ],
  // Title generation etc. → light OpenAI Pro model without priority-tier cost.
  // Luna is the light tier of the 5.6 generation (the same tier gpt-5.4-mini
  // held for 5.4), so this tracks the current generation at comparable cost.
  "small_model": "openai/gpt-5.6-luna",
  // Model split (2026-09-05). Two subscription pools, one per primary family:
  //   Claude Max x20 (sub account) — plan / review / debug / explain / deepsearch
  //     primaries (Fable 5.1; fall back to Opus 5 by hand), plus the
  //     Build-hot subagents (explore-medium/high/max, worker, reviewer,
  //     reviewer-deep on Sonnet 5 / Opus 5) so Build's own pool is not the only
  //     one its delegates draw on — and so Astra's code is reviewed cross-family.
  //   ChatGPT Pro x5 — build primary (GPT-6 Astra; fall back to Opus 5 by hand),
  //     debugger (Astra), and the cheap/bulky subagents (explore-spark/small,
  //     verifier, searcher/searcher-deep, ui-review, ux-persona, compaction).
  // GPT-6 Astra needs opencode >= 1.18.29: the ChatGPT-OAuth catalog filter
  // rejected integer major versions (`gpt-6`) before that. OpenCode never
  // falls back across models by itself — switch in the TUI when a pool is
  // exhausted. Pinned models here are defaults only: an explicit `--model`
  // (Hermes' engineer-pipeline always passes one) or a TUI pick overrides them.
  "agent": {
    "plan": {
      "color": "#f5f5f5",
      "model": "anthropic/claude-fable-5-1",
    },
    "build": {
      "color": "#ff6f00",
      "model": "openai/gpt-6-astra",
    },
    "review": {
      "color": "#A855F7",
    },
    "debug": {
      "color": "#06B6D4",
    },
    "explain": {
      "color": "#10B981",
    },
    "deepsearch": {
      "color": "#3B82F6",
    },
    // Context compaction fires on every long session; keep it off the
    // Claude Max window but on a model strong enough to preserve context.
    "compaction": {
      "model": "openai/gpt-5.6-terra",
      "variant": "medium",
    },
  },
  "provider": {
    "openai": {
      "models": {
        // OpenAI API pricing, USD per 1M tokens. Source: https://developers.openai.com/api/docs/pricing
        // GPT-5.6 tiers. Pro mode bills at the selected tier's standard rate;
        // OpenCode's gpt-5.6-pro alias resolves to Sol Pro.
        "gpt-5.6-sol": {
          "cost": {
            "input": 5,
            "cache_read": 0.5,
            "output": 30,
            "context_over_200k": {
              "input": 10,
              "cache_read": 1,
              "output": 45,
            },
          },
        },
        "gpt-5.6-terra": {
          "cost": {
            "input": 2.5,
            "cache_read": 0.25,
            "output": 15,
            "context_over_200k": {
              "input": 5,
              "cache_read": 0.5,
              "output": 22.5,
            },
          },
        },
        "gpt-5.6-luna": {
          "cost": {
            "input": 1,
            "cache_read": 0.1,
            "output": 6,
            "context_over_200k": {
              "input": 2,
              "cache_read": 0.2,
              "output": 9,
            },
          },
        },
        // Spark has no separate public API pricing table; use the published
        // gpt-5.3-codex rate so OpenCode cost estimates are not unknown.
        "gpt-5.3-codex-spark": {
          "cost": {
            "input": 1.75,
            "cache_read": 0.175,
            "output": 14,
          },
        },
      },
    },
  },
  "permission": {
    "edit": "ask",
    // .env may hold secrets — gate whole-file reads via the Read tool too
    // (bash cat/head/tail on .env is already denied below). Last match wins:
    // example/sample templates stay free.
    "read": {
      "*": "allow",
      "**/.env": "ask",
      "**/.env.*": "ask",
      "**/*.env": "ask",
      "**/.env.example": "allow",
      "**/.env.sample": "allow",
    },
    "bash": {
      "*": "ask",
      // Read / inspect (safe)
      "ls*": "allow",
      "cat *": "allow",
      "head *": "allow",
      "tail *": "allow",
      "tail -f*": "ask",
      "tail -F*": "ask",
      "wc *": "allow",
      "stat *": "allow",
      "file *": "allow",
      "du *": "allow",
      "df*": "allow",
      "grep *": "allow",
      "rg *": "allow",
      "jq *": "allow",
      "pwd*": "allow",
      "which *": "allow",
      "whoami*": "allow",
      "uname*": "allow",
      "date*": "allow",
      "echo *": "allow",
      "printf *": "allow",
      "realpath *": "allow",
      "dirname *": "allow",
      "basename *": "allow",
      "sort *": "allow",
      "uniq *": "allow",
      "cut *": "allow",
      "tr *": "allow",
      "nl *": "allow",
      "column *": "allow",
      "diff *": "allow",
      "comm *": "allow",
      "tree*": "allow",
      "type *": "allow",
      "command -v*": "allow",
      "printenv *": "allow",
      "base64 *": "allow",
      "xxd *": "allow",
      "shasum *": "allow",
      "md5 *": "allow",
      "mkdir *": "allow",
      "touch *": "allow",
      "tac *": "allow",
      "rev *": "allow",
      "paste *": "allow",
      "join *": "allow",
      "fold *": "allow",
      "fmt *": "allow",
      "expand *": "allow",
      "unexpand *": "allow",
      "seq *": "allow",
      "expr *": "allow",
      "cmp *": "allow",
      "od *": "allow",
      "hexdump *": "allow",
      "strings *": "allow",
      "egrep *": "allow",
      "fgrep *": "allow",
      "gron *": "allow",
      "cksum *": "allow",
      "sha1sum *": "allow",
      "sha256sum *": "allow",
      "sha512sum *": "allow",
      // System / network info
      "id*": "allow",
      "groups*": "allow",
      "hostname*": "allow",
      "uptime*": "allow",
      "arch*": "allow",
      "nproc*": "allow",
      "sw_vers*": "allow",
      "getconf *": "allow",
      "locale*": "allow",
      "cal*": "allow",
      "ps *": "allow",
      "pgrep *": "allow",
      "dig *": "allow",
      "host *": "allow",
      "nslookup *": "allow",
      "netstat*": "allow",
      "ss *": "allow",
      "ifconfig*": "allow",
      "ip *": "allow",
      "arp *": "allow",
      "scutil --dns*": "allow",
      // Docs
      "man *": "allow",
      "whatis *": "allow",
      "apropos *": "allow",
      "tldr *": "allow",
      // Git (non-destructive: allow / destructive & history-rewrite: ask)
      "git *": "allow",
      "git push*": "ask",
      "git rebase*": "ask",
      "git reset*": "ask",
      "git checkout*": "ask",
      "git restore*": "ask",
      "git clean*": "ask",
      "git commit*": "ask",
      "git merge*": "ask",
      "git revert*": "ask",
      "git cherry-pick*": "ask",
      "git config*": "ask",
      // Safe local commits (message-bearing) bypass the gate; bare `git commit` stays ask
      "git commit -m*": "allow",
      "git commit --message*": "allow",
      // Re-gate risky flags in ANY position (last match wins over the -m allows above)
      "git commit*--amend*": "ask",
      "git commit*--no-verify*": "ask",
      "git commit*--fixup*": "ask",
      "git commit*--squash*": "ask",
      "git commit*--all*": "ask",
      "git commit* -a*": "ask",
      "git commit* -n*": "ask",
      // Build / test (runs project code)
      "npm *": "allow",
      "pnpm *": "allow",
      "yarn *": "allow",
      "bun *": "allow",
      "make *": "allow",
      "cargo *": "allow",
      "go *": "allow",
      "pytest*": "allow",
      "jest*": "allow",
      "vitest*": "allow",
      "gh *": "allow",
      // Re-gate network-fetch-and-run / value-exposing forms back to ask
      "npm exec*": "ask",
      "npm create*": "ask",
      "pnpm dlx*": "ask",
      "pnpm exec*": "ask",
      "pnpm create*": "ask",
      "yarn dlx*": "ask",
      "yarn create*": "ask",
      "bun x*": "ask",
      "cargo install*": "ask",
      "go install*": "ask",
      "gh auth*": "ask",
      "gh secret*": "ask",
      // PR create/edit/view/ready stay allow; only merging is gated
      "gh pr merge*": "ask",
      "gh project delete*": "ask",
      "gh project field-delete*": "ask",
      "gh project item-delete*": "ask",
      // Note: npx / bunx / pip / uv / docker / node / python / ruby / bash -c stay ask by default
      // Version checks (gated tools)
      "node --version*": "allow",
      "node -v*": "allow",
      "deno --version*": "allow",
      "python --version*": "allow",
      "python -V*": "allow",
      "python3 --version*": "allow",
      "python3 -V*": "allow",
      "pip --version*": "allow",
      "pip list*": "allow",
      "pip show *": "allow",
      "pip3 --version*": "allow",
      "pip3 list*": "allow",
      "ruby --version*": "allow",
      "perl --version*": "allow",
      "docker --version*": "allow",
      "kubectl version*": "allow",
      "terraform version*": "allow",
      "terraform --version*": "allow",
      // Heavy tools (read-only subcommands)
      "docker ps*": "allow",
      "docker images*": "allow",
      "docker image ls*": "allow",
      "docker logs*": "allow",
      "docker logs -f*": "ask",
      "docker logs --follow*": "ask",
      "docker inspect*": "allow",
      "docker info*": "allow",
      "docker compose ls*": "allow",
      "docker compose ps*": "allow",
      "kubectl get*": "allow",
      "kubectl describe*": "allow",
      "kubectl logs*": "allow",
      "kubectl logs -f*": "ask",
      "kubectl logs --follow*": "ask",
      "kubectl config view*": "allow",
      "kubectl config view --raw*": "ask",
      "kubectl top*": "allow",
      "kubectl explain*": "allow",
      "brew list*": "allow",
      "brew info*": "allow",
      "brew search*": "allow",
      "brew outdated*": "allow",
      "brew config*": "allow",
      "brew doctor*": "allow",
      "brew --version*": "allow",
      "terraform validate*": "allow",
      "terraform plan*": "allow",
      "terraform show*": "allow",
      "terraform output*": "allow",
      "terraform state list*": "allow",
      "terraform fmt -check*": "allow",
      // Agent Browser
      "agent-browser *": "allow",
      // Secret (read-only subcommands)
      "secret ls*": "allow",
      "secret list*": "allow",
      "secret show *": "allow",
      "secret projects*": "allow",
      "secret help*": "allow",
      "secret keychain ls*": "allow",
      "secret keychain master status*": "allow",
      // Deny block pinned last (not overridden by allows above)
      "sudo *": "deny",
      "wget *": "deny",
      "dd *": "deny",
      "mkfs*": "deny",
      "shutdown*": "deny",
      "reboot*": "deny",
      "halt*": "deny",
      "diskutil erase*": "deny",
      ":(){ :|:& }; *": "deny",
      // Block reading .env via bash (best-effort)
      "cat *.env": "deny",
      "head *.env": "deny",
      "tail *.env": "deny",
      "cat *.env.*": "deny",
      // Secret: destructive / value-exposing
      "secret get*": "deny",
      "secret rm*": "deny",
      "secret del*": "deny",
      "secret export*": "deny",
      "secret import*": "deny",
      "secret keychain rm*": "deny",
      "secret keychain delete*": "deny",
      "secret keychain master set*": "deny",
      "secret keychain master rotate*": "deny",
      "secret keychain master forget*": "deny",
      "secret keychain master reveal*": "deny",
    },
    "skill": {
      "*": "ask",
      "customize-opencode": "allow",
      "keychain-secrets": "allow",
      "approach*": "allow",
      "resolve-dependabot-alerts": "allow",
      "manage-github-projects": "allow",
      "git-*": "allow",
      "web-ui": "allow",
      "ux-persona-testing": "allow",
      "japanese-*": "allow",
    },
    // Custom GitHub Project tools (tools/github_project.ts) — board + issue-lifecycle ops, no delete
    "github_project_create": "allow",
    "github_project_field_ensure": "allow",
    "github_project_item_add": "allow",
    "github_project_item_set": "allow",
    "github_project_item_list": "allow",
    "github_project_item_note": "allow",
    "github_project_item_promote": "allow",
    "github_project_view_ensure": "allow",
    "github_project_issue_link": "allow",
    "github_project_issue_develop": "allow",
    // Custom git workflow tools (tools/git.ts) — read/index only, no commit/push/merge
    "git_secret_scan": "allow",
    "git_stage_hunks": "allow",
    "git_provenance": "allow",
    "git_related_scan": "allow",
    "git_history_digest": "allow",
    "git_amend_check": "allow",
    "git_commit_lint": "allow",
    // Custom X search tool (tools/x.ts) — xAI server-side x_search, read-only external query
    "x_search": "allow",
  },
  "mcp": {
    "blender-mcp": {
      "type": "local",
      "command": [
        "uvx",
        "blender-mcp"
      ],
      "enabled": false,
      "environment": {
        "BLENDER_HOST": "localhost",
        "BLENDER_PORT": "9876"
      }
    },
    "21st-dev": {
      "type": "local",
      "command": [
        "npx",
        "-y",
        "@21st-dev/magic@latest"
      ],
      "enabled": false,
      "environment": {
        "API_KEY": "{env:MAGIC_API_KEY}"
      }
    }
  }
}
